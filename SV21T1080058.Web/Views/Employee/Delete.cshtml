@model Employee
@{
    ViewBag.Title = "Xóa Nhân viên";
}

<div class="box box-danger">
    <div class="box-body">
        <form class="form-horizontal" action="~/Employee/Delete/@Model.EmployeeID" method="post">
            <div class="form-group">
                <label class="control-label col-sm-2">Họ và tên: </label>
                <p class="form-control-static">@Model.FullName</p>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Ngày sinh: </label>
                <p class="form-control-static">@Model.BirthDate</p>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Địa chỉ: </label>
                <p class="form-control-static">@Model.Address</p>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Điện thoại: </label>
                <p class="form-control-static">@Model.Phone</p>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Email: </label>
                <p class="form-control-static">@Model.Email</p>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Trạng thái: </label>
                <p class="form-control-static">@(Model.IsWorking ? "Nhân viên đang làm việc":"Nhân viên dừng làm việc")</p>
            </div>
            <div class="form-group">
                <div class="col-lg-offset-2 col-sm-10">
                    <img id="Photo" src="~/images/employees/@Model.Photo" class="img img-bordered" style="width:200px" alt="NoPhoto" />
                </div>
                
            </div>
            
            <div class="form-group">
                <div class="col-lg-offset-2 col-sm-10">
                    @if (ViewBag.AllowDelete)
                    {
                        <button type="submit" class="btn btn-danger">
                            <i class="fa fa-trash"></i>
                            Xóa dữ liệu
                        </button>
                    }
                    else
                    {
                        <span style="color:red">Không được xóa Nhân viên này!</span>
                    }

                    <a href="~/Employee" class="btn btn-default">
                        Quay lại
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>
