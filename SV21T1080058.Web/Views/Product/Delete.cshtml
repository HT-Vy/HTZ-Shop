@model Product
﻿@{
    ViewBag.Title = "Xóa Mặt hàng";
}

<div class="box box-danger">

    <div class="box-header with-border ">
        <h3 class="box-title">Thông tin mặt hàng</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>

    <div class="box-body">
        <form class="form-horizontal" action="~/Product/Delete/@Model.ProductID" method="post" enctype="multipart/form-data">

            <div class="form-group">
                <label class="control-label col-sm-2">Tên mặt hàng:</label>
                <p class="form-control-static">@Model.ProductName</p>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Mô tả:</label>
                <p class="form-control-static">@Model.ProductDescription</p>

            </div>

         
            <div class="form-group">
                <label class="control-label col-sm-2">Đơn vị tính:</label>
                <p class="form-control-static">@Model.Unit</p>

            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Giá hàng:</label>
                <p class="form-control-static">@Model.Price.ToString("N0")</p>

            </div>

            
            <div class="form-group">
                <label class="control-label col-sm-2">Ảnh minh họa:</label>
                <div class="col-sm-10">
                    <img id="photo" class="img-bordered col-sm-10" src="~/images/products/@Model.Photo" style="margin-top: 10px; width: 150px;" alt="NoPhoto" />
                </div>
            </div>
            

            <div class="form-group text-center">
                @if (ViewBag.AllowDelete)
                {
                    <button type="submit" class="btn btn-danger">
                        <i class="fa fa-trash"></i>Xóa dữ liệu
                    </button>
                }
                else
                {
                    <span style="color:red">Không được phép xóa mặt hàng này!</span>
                }
                <a href="~/Product" class="btn btn-default"> Quay lại</a>
            </div>

        </form>

    </div>

</div>


